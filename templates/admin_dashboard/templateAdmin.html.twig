{% extends 'base.html.twig' %}

{% block title %}Hello AdminDashboardController!
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-2 dashboard-menu">
			<a {% if route_name == "homepage" %} class="active" {% else %} class="" {% endif %} href="{{ path('admin_home') }}">Accueil<span class="sr-only">(current)</span>
			</a>


			<a {% if route_name == "homepage" %} class="active" {% else %} class="" {% endif %} href="{{ path('admin_articles') }}">Articles<span class="sr-only">(current)</span>
			</a>


			<a {% if route_name == "homepage" %} class="active" {% else %} class="" {% endif %} href="{{ path('admin_comments') }}">
				Gérer les commentaires<span class="sr-only">(current)</span>
			</a>
		</div>
		<div class="col-10">
			{% block content %}{% endblock %}
		</div>
	</div>
{% endblock %}
