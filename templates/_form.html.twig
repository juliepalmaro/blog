<form method="post">
	<h1>{{ form_title|default('Formulaire') }}</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-lg btn-primary" type="submit">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</form>
