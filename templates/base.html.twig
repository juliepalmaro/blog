<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/bootstrap/bootstrap.min.css') }}">
        {% endblock %}
    </head>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #6AA9C1;">
            <a class="navbar-brand" href="{{ path('homepage') }}">LARAJ</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-item nav-link active" href="#">Accueil <span class="sr-only">(current)</span></a>
                    <a class="nav-item nav-link" href="#">A propos</a>
                    <a class="nav-item nav-link" href="{{ path('contact') }}">Contact</a>
                    <a class="nav-item nav-link" href="{{ path('contact') }}">Créer un compte</a>
                    <a class="nav-item nav-link" href="{{ path('contact') }}">Se connecter</a>
                </div>
            </div>
        </nav>
    </header>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}
            <script src="{{ asset('styles/js/jquery-3.2.1.slim.min.js') }}"></script>
            <script src="{{ asset('styles/js/popper.min.js') }}"></script>
            <script src="{{ asset('styles/js/bootstrap.min.js') }}"></script>
        {% endblock %}
    </body>
</html>
